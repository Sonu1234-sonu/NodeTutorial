export const Register=(req,res)=>{
    res.json({messsage:"User Register Done"});
};

export const Login=(req,res)=>{
    res.json({messsage:"User Login Done"});
};

export const Logout=(req,res)=>{
    res.json({messsage:"User Logout Done"});
};